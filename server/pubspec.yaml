name: server
description: An empty Aqueduct application.
version: 0.0.1
author: stable|kernel <jobs@stablekernel.com>

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  aqueduct: ^3.0.0
  crypto: ^2.0.6
  http: ^0.12.0+1
  xml: ^3.4.1
  js: ^0.6.1+1

dev_dependencies:
  test: ^1.0.0
  aqueduct_test: ^1.0.0
